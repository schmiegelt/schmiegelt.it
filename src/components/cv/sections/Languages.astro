---
import Section from "../Section.astro";

import { languages } from "@cv";
import { certificates } from "@cv";
---

<Section className={Astro.props.className} title="Languages">
  <ul class="space-y-4 py-3 print:space-y-0">
    {
      languages.map(({ language, fluency }) => {
        return (
          <div class="flex items-baseline">
            <div class="relative w-full items-baseline justify-between gap-2">
              <h3 class="mr-6 items-baseline gap-x-2 gap-y-0.5 font-medium">
                <div class="hover:text-skin-hue group flex items-center gap-[6px] decoration-dotted underline-offset-[5px]">
                  {language}
                </div>
              </h3>
              <div class="right-0 top-0 text-xs text-skin-muted md:absolute md:block">
                {fluency}
              </div>
            </div>
          </div>
        );
      })
    }
  </ul>
</Section>
